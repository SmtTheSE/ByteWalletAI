version: "3.8"

services:
  bytewallet-ai:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AI_DEFAULT_MODE=rules_only
    # Note: For production, pass GEMINI_API_KEY via a .env file or secret
    # env_file:
    #   - .env
